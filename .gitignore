# Ignore the original huge dataset (not suitable for GitHub)
backend/data/sales_data.csv

# Allow smaller "render" dataset
# (do NOT ignore backend/data/*.csv anymore)
__pycache__/
*.pyc
.env
git add .gitignore
